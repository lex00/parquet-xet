# Generated by chant — see ci/pipeline.ts
# https://intentius.io/chant
name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  notebook:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install pyarrow numpy matplotlib jupyter nbconvert
      - name: Execute notebook
        run: jupyter nbconvert --to notebook --execute parquet-xet-write-strategy.ipynb --output parquet-xet-write-strategy.ipynb
